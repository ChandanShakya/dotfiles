#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc
[[ -z $DISPLAY && $XDG_VTNR -eq 1 ]] && exec startx
# run the customexec display.sh file which is in /home/chandan/sysprog/CustomExec/display.sh 
# it contatins shell scripts
source /home/chandan/sysprog/CustomExec/display.sh
